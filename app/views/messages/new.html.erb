<%= form_for(@message) do |f| %>
    <div class="field">
      <%= f.label :message %><br>
      <%= f.text_area :content %>
    </div>
    <% if current_user %>
        <div class="actions">
      <%= f.submit "Create" %>
      </div>
    <% end %>

<% end %>